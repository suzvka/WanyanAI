# Operations Config

`published/` 目录用于存放当前已发布配置文件。

发布方式：
1. 团队确认配置内容。
2. 系统管理员将定稿文件上传到服务器上的 `ops-config/published/`。
3. 前台请求时读取已发布配置；若读取失败则回退到内置默认配置。

当前阶段：
- 一期仅支持启停与默认值配置
- 提示词模板配置化与版本回滚在二期处理
- 输入结构与提示词拼装结果仅用于系统内部流转，不对用户暴露
