# DESIGN.md

## 主题与明暗模式

- 全局通过 `next-themes`（`attribute="class"`，默认跟随系统）驱动 `.dark` 变量体系。
- Admin 管理控制台（`/admin`，含登录页）支持深色模式适配：
  - 页面背景、头部使用语义 token（`bg-background`），不硬编码浅色。
  - 头部提供主题切换按钮（Sun/Moon 图标，`useTheme` 切换，Hydration 安全）。

## Admin 管理控制台（/admin）

- **信息架构（心智模型）**：两级结构——左侧「站点导航」（对象级）+ 右侧「配置编辑」（操作级），替代原横向按钮流；当前站点在主内容顶部以身份卡呈现（图标 + 名称 + ID + 能力徽章）。
- **布局**：桌面端为 `260px 侧边栏 + 主内容区` 双栏；移动端侧边栏隐藏，降级为顶部横向站点切换 chips。
- **配置交互**：
  - 添加模型用 Dialog 表单、删除用 AlertDialog 确认、操作结果用全局 sonner toast 反馈（替代原生 prompt/confirm/alert）。
  - 凭证配置以可折叠模型卡片分组（Collapsible），字段表单两列栅格；底部 Sticky 浮动保存栏，未保存时显示"有未保存的修改"提示。
  - 模型启停为状态化列表：每行含状态徽章（运行中=绿色 / 已停用=中性灰）+ Switch，顶部统计启用数/总数，单行即时保存。
- **视觉**：卡片圆角（rounded-2xl）+ 语义阴影（shadow-card）；状态用绿/灰语义色，禁用原生红色默认按钮。
