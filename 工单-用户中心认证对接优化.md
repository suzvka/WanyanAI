# 工单：用户中心认证对接优化

**发起方**：我方项目组  
**接收方**：用户中心项目组  
**优先级**：高  
**关联问题**：登录回跳丢失（postMessage 静默丢弃）

---

## 一、当前对接方式与问题

### 1.1 当前方案

我方通过 **弹窗 + postMessage** 方式接入用户中心登录：

1. 我方打开弹窗 → `{userCenterUrl}/embed/sign-in`
2. 用户在弹窗中完成认证
3. 用户中心通过 `postMessage` 向父窗口发送 `auth:sign-in:success` 事件，附带 `token` 和 `user` 信息
4. 父窗口收到消息后，调用我方后端签发 station token

### 1.2 存在的问题

我方使用 `e.origin` 校验 postMessage 消息来源以确保安全。但实际运行中发现：

- 用户中心域名 `https://login.example.com` 会 **301 重定向** 到后台域名 `https://login-backend.internal.com`
- 重定向后弹窗页面的实际 `origin` 变为后台域名，与配置的 `userCenterUrl` 不匹配
- 我方 postMessage 的 origin 校验失败，消息被静默丢弃
- 后果：用户中心明明显示"登录成功"，我方页面仍显示"未登录"，且无任何日志可查

> 我方已通过新增 `USER_CENTER_ALLOWED_ORIGINS` 白名单临时解决此问题，但底层设计缺陷仍然存在。

---

## 二、请求确认

### 2.1 用户中心是否支持 OAuth 2.0 Authorization Code + PKCE 流程？

这是行业标准的跨域认证方案，可从根本上解决 `origin` 依赖问题：

```
我方前端 → 302 跳转至用户中心认证页
用户中心 → 用户认证 → 302 跳回我方回调地址（携带 code）
我方前端 → 将 code 发给后端
我方后端 → server-to-server 交换 code → token
```

**优势**：
- 安全边界由令牌加密签名保证，**不依赖 HTTP `origin` 字段**
- 令牌交换走服务端（server-to-server），`code` 一次性有效
- 不受域名重定向/变更影响
- 可完整审计，日志可追溯

### 2.2 如果不支持 OAuth 2.0，是否有其他推荐的对接方案？

如果用户中心短期内无法支持 OAuth 2.0，请提供以下信息：

1. **postMessage 的 origin 是否有保证与 `userCenterUrl` 一致？**
   - 如果域名重定向是不可避免的设计，能否在重定向后保持 `origin` 不变（如使用 CNAME 而非 301）？
   - 或者，能否提供所有可能的 origin 列表供我方配置？

2. **是否支持令牌直验（token introspection）？**
   - 我方能否直接调用用户中心的 **token 校验接口**（server-to-server）来验证 `token` 的有效性？
   - 如果支持，请提供接口文档（URL、请求/响应格式、鉴权方式）

3. **是否有计划支持 OAuth 2.0？**
   - 如有时间规划，请告知预计上线时间，我方将配合适配

---

## 三、我方建议的对接方案（按优先级排序）

| 优先级 | 方案 | 说明 |
|--------|------|------|
| **P0** | OAuth 2.0 Authorization Code + PKCE | 行业标准，彻底解决 origin 依赖 |
| **P1** | 提供 token 校验接口（server-to-server） | 我方放弃 origin 校验，改为服务端验证令牌真实性 |
| **P2** | 保证 postMessage origin 与配置域名一致 | 消除域名重定向，或提供明确的 origin 白名单 |

---

## 四、联系方式

请将以上问题反馈给用户中心团队，期待回复。

**我方对接人**：摇风  
**我方项目**：AI 文本分析平台