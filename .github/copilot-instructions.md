# Copilot Instructions

## Project Guidelines
- 在开始下一步开发前，需要先对齐当前已有模块，即使现阶段模块行为仍可保持模拟实现。
- 当前环境没有可用的本地编译工具链；需要由用户手动将代码移到服务器后再执行构建验证。
- 本项目的分析请求架构应改为：服务端仅提供提示词模板资源，不接触用户令牌；客户端按评价方式获取带槽位的提示词并自行拼接最终提示词后直接调用外部 API。